{
  "name": "smarttable-nyc-reservations",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Smarttable MVP with Supabase-backed restaurant offers and reservations.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check src/app-core.js && node --check api/index.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
